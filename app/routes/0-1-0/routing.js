module.exports = function(router, myData) {

    var version = "0-1-0";

    // Route index page
    // router.get('/' + version + '/bta-homepage', function (req, res) {
    //   res.render(version + '/bta-homepage', {
    //     myData: req.session.myData
    //   });
}
